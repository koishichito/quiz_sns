# --- アプリ本体 ---
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
PyMySQL>=1.1            # MySQL用の同期ドライバ(純Python)
cryptography>=42        # MySQL 8 既定の caching_sha2_password 認証と、
                        # KeycloakトークンのRS256署名検証の両方で必須
PyJWT>=2.9              # Keycloakが発行したJWTの検証(JWKS取得のPyJWKClient含む)。
                        # python-jose は使わない(メンテ停止のため)
pydantic-settings>=2.4  # .env から設定を読む
# bcrypt と python-multipart は削除した:
#   パスワード保管もフォーム形式のログイン受付も Keycloak の仕事になり、
#   アプリがパスワードに触れる経路ごと消えたため

# --- テスト ---
pytest>=8.0
httpx>=0.27             # TestClient の実体
